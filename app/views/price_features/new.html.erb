<%= render 'shared/wrap' %>		
<% if policy(@user_price_features).new? %>
	<%= form_tag "/users/#{current_user.id}/price_features", method: :post do %>
    <%= render 'feature_types_form' %>  
    <br>
    <br>   
  <%= submit_tag "Submit" %>
  <% end %>
<% end %>
