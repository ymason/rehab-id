<%= form_for(appointment) do |f| %>
  <% if appointment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appointment.errors.count, "error") %> prohibited this appointment from being saved:</h2>

      <ul>
      <% appointment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :phone_number %>
    <%= f.text_field :phone_number %>
  </div>

  <div class="field">
    <%= f.label :time %>
    <%= f.datetime_select :time %>
  </div>

  <div class="field">
    <%= f.label :time_zone %>
    <%= f.text_field :time_zone %>
  </div>

  <div class="field">
    <%= f.label :bid_id %>
    <%= f.text_field :bid_id %>
  </div>

  <div class="field">
    <%= f.label :contractor_bid_id %>
    <%= f.text_field :contractor_bid_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
